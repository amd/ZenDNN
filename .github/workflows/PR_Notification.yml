name: PR Status Notification (Main Branch)
on:
  pull_request:
    branches: [main]
    types: [opened, reopened, synchronize]

permissions:
  checks: read
  pull-requests: read
  contents: read
  actions: read

jobs:
  notify_teams:
    runs-on: amd-zenai-arc-xsmall-dind
    timeout-minutes: 120
    if: github.actor != 'github-actions[bot]'
    
    steps:
      - name: Get PR info
        id: pr_info
        uses: actions/github-script@v7
        with:
          script: |
            const pr = context.payload.pull_request;
            
            return {
              number: pr.number,
              title: pr.title || '',
              url: pr.html_url || '',
              head_sha: pr.head.sha,
              author: pr.user?.login || '',
              base_ref: pr.base?.ref || '',
              head_ref: pr.head?.ref || ''
            };

      - name: Find Triggered External Workflows
        id: find_external
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.ZIAIE_PAT }}
          script: |
            const pr = JSON.parse(`${{ steps.pr_info.outputs.result }}`);
            
            console.log(`🎯 Looking for workflows triggered by PR #${pr.number}`);
            console.log(`Expected: 3 external workflows (2 in PyTorch + 1 in TensorFlow)`);
            
            try {
              // Wait for triggers to process
              console.log('⏳ Waiting 3 minutes for workflow triggers...');
              await new Promise(resolve => setTimeout(resolve, 180000));
              
              const matchedWorkflows = [];
              
              // Define external repositories with expected triggers
              const externalRepos = [
                {
                  owner: 'AMD-Zenai',
                  repo: 'ZenDNN_PyTorch_Plugin',
                  name: 'ZenDNN PyTorch Plugin',
                  expectedTriggers: [
                    { event: 'zendnn-pr', workflowName: 'ZenDNN PyTorch CI' },
                    { event: 'zendnn-zentorch-zendnnl-pr', workflowName: 'ZenTorch ZenDNNL CI' }
                  ]
                },
                {
                  owner: 'AMD-Zenai', 
                  repo: 'ZenDNN_TensorFlow_Plugin',
                  name: 'ZenDNN TensorFlow Plugin',
                  expectedTriggers: [
                    { event: 'zendnn-pr', workflowName: 'ZenDNN TensorFlow CI' }
                  ]
                }
              ];
              
              // Check each external repository
              for (const repoInfo of externalRepos) {
                console.log(`\n🔍 Checking ${repoInfo.name} for PR #${pr.number}...`);
                console.log(`   Expected triggers: ${repoInfo.expectedTriggers.map(t => t.event).join(', ')}`);
                
                try {
                  // Get recent workflow runs (last 45 minutes)
                  const response = await github.rest.actions.listWorkflowRunsForRepo({
                    owner: repoInfo.owner,
                    repo: repoInfo.repo,
                    per_page: 50
                  });
                  
                  const recentRuns = response.data.workflow_runs.filter(run => {
                    const age = (Date.now() - new Date(run.created_at)) / (1000 * 60);
                    return run.event === 'repository_dispatch' && age <= 45;
                  });
                  
                  console.log(`  Found ${recentRuns.length} recent dispatch runs`);
                  
                  // Check each run for PR number match in logs
                  for (const run of recentRuns) {
                    const age = Math.round((Date.now() - new Date(run.created_at)) / (1000 * 60));
                    const runTitle = run.display_title || run.name || '';
                    
                    console.log(`    🔍 Checking run: "${runTitle}" (${age}m ago)`);
                    
                    try {
                      // Get jobs for this workflow run
                      const jobsResp = await github.rest.actions.listJobsForWorkflowRun({
                        owner: repoInfo.owner,
                        repo: repoInfo.repo,
                        run_id: run.id
                      });
                      
                      let foundMatch = false;
                      let triggerType = 'Unknown';
                      
                      // Check each job's logs for PR details
                      for (const job of jobsResp.data.jobs) {
                        if (foundMatch) break;
                        
                        try {
                          console.log(`      📄 Checking job: ${job.name}`);
                          
                          const logsResp = await github.rest.actions.downloadJobLogsForWorkflowRun({
                            owner: repoInfo.owner,
                            repo: repoInfo.repo,
                            job_id: job.id
                          });
                          
                          const logsText = logsResp.data.toString();
                          
                          // ✅ ENHANCED: Search for PR details in "display triggering PR information" step
                          const prPatterns = [
                            `PR Number: #${pr.number}`,
                            `🔵 TRIGGERED BY ZENDNN PR:.*PR Number: #${pr.number}`,
                            `Repository: AMD-Zenai/ZenDNN.*PR Number: #${pr.number}`,
                            `https://github.com/AMD-Zenai/ZenDNN/pull/${pr.number}`
                          ];
                          
                          // Check if any pattern matches
                          const matchedPattern = prPatterns.find(pattern => 
                            new RegExp(pattern, 'is').test(logsText)
                          );
                          
                          if (matchedPattern) {
                            console.log(`        ✅ FOUND PR #${pr.number} in logs!`);
                            console.log(`        📍 Matched pattern: ${matchedPattern}`);
                            
                            // Determine trigger type from logs
                            if (logsText.includes('Dispatch Type: zendnn-zentorch-zendnnl-pr') || 
                                logsText.includes('zendnn-zentorch-zendnnl-pr')) {
                              triggerType = 'ZenTorch ZenDNNL';
                            } else if (logsText.includes('Dispatch Type: zendnn-pr')) {
                              if (repoInfo.repo === 'ZenDNN_PyTorch_Plugin') {
                                triggerType = 'ZenDNN PyTorch';
                              } else if (repoInfo.repo === 'ZenDNN_TensorFlow_Plugin') {
                                triggerType = 'ZenDNN TensorFlow';
                              }
                            }
                            
                            foundMatch = true;
                            break;
                          }
                          
                        } catch (logError) {
                          console.log(`        ❌ Could not get logs for job ${job.id}: ${logError.message}`);
                        }
                      }
                      
                      // If match found, add to results
                      if (foundMatch) {
                        console.log(`      🎯 MATCH CONFIRMED: ${triggerType} workflow for PR #${pr.number}`);
                        
                        matchedWorkflows.push({
                          id: run.id,
                          name: `${repoInfo.name} (${triggerType})`,
                          status: run.conclusion || run.status,
                          url: run.html_url,
                          repository: `${repoInfo.owner}/${repoInfo.repo}`,
                          workflow_name: runTitle,
                          trigger_type: triggerType
                        });
                      } else {
                        console.log(`      ❌ No PR #${pr.number} reference found in this run`);
                      }
                      
                    } catch (jobError) {
                      console.log(`      ❌ Could not get jobs for run ${run.id}: ${jobError.message}`);
                    }
                  }
                  
                } catch (repoError) {
                  console.log(`❌ Error accessing ${repoInfo.name}: ${repoError.message}`);
                }
              }
              
              console.log(`\n🎯 FINAL RESULT: Found ${matchedWorkflows.length}/3 expected workflows for PR #${pr.number}`);
              if (matchedWorkflows.length > 0) {
                console.log(`✅ MATCHED WORKFLOWS:`);
                matchedWorkflows.forEach(wf => {
                  console.log(`  ✅ ${wf.name}: ${wf.status} - ${wf.url}`);
                });
              } else {
                console.log(`❌ NO MATCHING WORKFLOWS FOUND`);
                console.log(`   This could mean:`);
                console.log(`   1. Workflows are still starting up`);
                console.log(`   2. Triggers failed to fire`);
                console.log(`   3. External repositories are not accessible`);
              }
              
              // Log if we're missing any expected workflows
              if (matchedWorkflows.length < 3) {
                console.log(`\n⚠️  Expected 3 workflows, found ${matchedWorkflows.length}`);
                const missing = 3 - matchedWorkflows.length;
                console.log(`   Missing ${missing} workflow(s) - will wait for them to appear`);
              }
              
              return {
                found: matchedWorkflows.length > 0,
                external_workflows: matchedWorkflows,
                expected_count: 3,
                found_count: matchedWorkflows.length
              };
              
            } catch (error) {
              console.log(`❌ Error: ${error.message}`);
              return { 
                found: false, 
                external_workflows: [],
                expected_count: 3,
                found_count: 0
              };
            }

      - name: Wait for ALL workflows and checks to complete
        id: wait_for_completion
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.ZIAIE_PAT }}
          script: |
            const pr = JSON.parse(`${{ steps.pr_info.outputs.result }}`);
            
            // Safe parsing of external workflows result
            let externalResult = { found: false, external_workflows: [] };
            try {
              const externalOutput = `${{ steps.find_external.outputs.result || '{}' }}`;
              externalResult = externalOutput ? JSON.parse(externalOutput) : { found: false, external_workflows: [] };
            } catch (error) {
              console.log(`Could not parse external result: ${error.message}`);
              externalResult = { found: false, external_workflows: [] };
            }
            
            const owner = context.repo.owner;
            const repo = context.repo.repo;
            const ref = pr.head_sha;

            console.log(`🔍 Monitoring ALL workflows and checks for PR #${pr.number}, commit: ${ref}`);
            console.log(`External workflows found: ${externalResult.external_workflows.length}`);
            
            const checkAllWorkflowsAndChecks = async () => {
              try {
                console.log(`📊 Checking ALL workflows and checks for commit: ${ref.substring(0, 7)}`);

                // ✅ Get ALL LOCAL workflow runs for this commit
                const workflowResp = await github.rest.actions.listWorkflowRunsForRepo({
                  owner,
                  repo,
                  head_sha: ref,
                  per_page: 100
                });
                
                const workflows = (workflowResp.data.workflow_runs || [])
                  .filter(run => {
                    const isSelf = run.id === context.runId;
                    const isNotification = run.name.toLowerCase().includes('notification') || 
                                         run.name.toLowerCase().includes('notify') ||
                                         run.name.toLowerCase().includes('pr status');
                    
                    return !isSelf && !isNotification;
                  });

                // ✅ Get ALL LOCAL check runs for this commit
                const checkResp = await github.rest.checks.listForRef({
                  owner,
                  repo,
                  ref,
                  per_page: 100
                });
                
                const checks = (checkResp.data.check_runs || [])
                  .filter(check => {
                    const isNotification = check.name.toLowerCase().includes('notify') || 
                                         check.name.toLowerCase().includes('notification') ||
                                         check.name.toLowerCase().includes('pr status');
                    
                    return !isNotification;
                  });

                // ✅ Get ALL status checks for this commit
                const statusResp = await github.rest.repos.getCombinedStatusForRef({
                  owner,
                  repo,
                  ref
                });
                
                const statuses = statusResp.data.statuses || [];

                // Combine all LOCAL workflow information
                const allRuns = [];
                
                // Add ALL local workflows
                workflows.forEach(run => {
                  allRuns.push({
                    id: run.id,
                    name: run.name,
                    status: run.conclusion || run.status,
                    url: run.html_url,
                    type: 'local_workflow_run',
                    repository: `${owner}/${repo}`
                  });
                });

                // Add ALL local checks (avoid duplicates)
                checks.forEach(check => {
                  if (!allRuns.find(run => run.name === check.name)) {
                    allRuns.push({
                      id: check.id,
                      name: check.name,
                      status: check.conclusion || check.status,
                      url: check.html_url,
                      type: 'local_check_run',
                      repository: `${owner}/${repo}`
                    });
                  }
                });

                // Add ALL status checks (avoid duplicates)
                statuses.forEach(status => {
                  if (!allRuns.find(run => run.name === status.context)) {
                    allRuns.push({
                      id: status.id,
                      name: status.context,
                      status: status.state,
                      url: status.target_url || status.url,
                      type: 'local_status_check',
                      repository: `${owner}/${repo}`
                    });
                  }
                });

                // Add MATCHED external workflows and update their status
                for (const extWorkflow of externalResult.external_workflows) {
                  try {
                    // Get updated status for external workflow
                    const [extOwner, extRepo] = extWorkflow.repository.split('/');
                    const externalResp = await github.rest.actions.getWorkflowRun({
                      owner: extOwner,
                      repo: extRepo,
                      run_id: extWorkflow.id
                    });
                    
                    allRuns.push({
                      id: externalResp.data.id,
                      name: `🌐 ${extWorkflow.name}`,
                      status: externalResp.data.conclusion || externalResp.data.status,
                      url: externalResp.data.html_url,
                      type: 'external_workflow_run',
                      repository: extWorkflow.repository
                    });
                    
                  } catch (extError) {
                    console.log(`❌ Could not update external workflow ${extWorkflow.id}: ${extError.message}`);
                    // Add with last known status
                    allRuns.push({
                      id: extWorkflow.id,
                      name: `🌐 ${extWorkflow.name}`,
                      status: extWorkflow.status || 'unknown',
                      url: extWorkflow.url,
                      type: 'external_workflow_run',
                      repository: extWorkflow.repository
                    });
                  }
                }

                // ✅ CRITICAL: Only consider FINAL states as completed
                const running = allRuns.filter(r => 
                  ['in_progress', 'queued', 'pending', 'waiting', 'requested'].includes(r.status)
                );
                
                const completed = allRuns.filter(r => 
                  ['success', 'failure', 'cancelled', 'skipped', 'error', 'neutral'].includes(r.status)
                );

                // Detailed status logging
                console.log(`\n📋 COMPLETE STATUS SUMMARY:`);
                console.log(`Total workflows/checks: ${allRuns.length}`);
                console.log(`  - Workflows: ${workflows.length}`);
                console.log(`  - Check runs: ${checks.length}`);
                console.log(`  - Status checks: ${statuses.length}`);
                console.log(`  - External workflows: ${externalResult.external_workflows.length}`);
                console.log(`Completed: ${completed.length}`);
                console.log(`Still running: ${running.length}`);

                console.log(`\n📊 ALL WORKFLOWS AND CHECKS (LOCAL + EXTERNAL):`);
                allRuns.forEach(run => {
                  const statusIcon = run.status === 'success' ? '✅' : 
                                    run.status === 'failure' ? '❌' : 
                                    ['in_progress', 'pending', 'queued'].includes(run.status) ? '⏳' : 
                                    run.status === 'cancelled' ? '🟠' :
                                    run.status === 'skipped' ? '⏭️' : 
                                    run.status === 'neutral' ? '⚪' :
                                    run.status === 'error' ? '🔴' : '🔄';
                  
                  const typeLabel = run.type.includes('external') ? ` [${run.repository}]` : 
                                   run.type.includes('status') ? ' [status]' :
                                   run.type.includes('check') ? ' [check]' : ' [workflow]';
                  console.log(`  ${statusIcon} ${run.name}${typeLabel}: ${run.status.toUpperCase()}`);
                });
                
                if (running.length > 0) {
                  console.log(`\n⏳ STILL WAITING FOR:`);
                  running.forEach(r => {
                    console.log(`   🔄 ${r.name}: ${r.status}`);
                  });
                }

                // ✅ CRITICAL: ALL must be completed (no running workflows/checks)
                const allComplete = running.length === 0 && allRuns.length > 0;
                
                if (allComplete) {
                  console.log(`\n🎉 ALL ${allRuns.length} WORKFLOWS AND CHECKS COMPLETED!`);
                } else {
                  console.log(`\n⏳ Waiting for ${running.length} workflow(s)/check(s) to complete...`);
                }

                return {
                  all_complete: allComplete,
                  workflows: allRuns,
                  running_count: running.length,
                  completed_count: completed.length,
                  total_count: allRuns.length,
                  external_count: externalResult.external_workflows.length,
                  workflow_count: workflows.length,
                  check_count: checks.length,
                  status_count: statuses.length,
                  currently_running: running.map(r => r.name)
                };
                
              } catch (error) {
                console.log(`❌ Error checking workflows and checks: ${error.message}`);
                return { 
                  all_complete: false,
                  workflows: [], 
                  error: error.message,
                  external_count: 0
                };
              }
            };

            // Wait for workflows to initialize
            console.log('⏳ Waiting 4 minutes for all workflows and checks to initialize...');
            await new Promise(resolve => setTimeout(resolve, 240000));  // 4 minutes

            let result = await checkAllWorkflowsAndChecks();
            let attempts = 0;
            const maxAttempts = 60;  // 60 minutes max wait for main branch
            
            console.log(`\n🔍 INITIAL CHECK RESULTS:`);
            console.log(`Found ${result.total_count} workflows/checks (${result.external_count} external), ${result.running_count} still running`);

            // ✅ CRITICAL: Keep waiting until ALL are complete (no forced completion)
            while (!result.all_complete && attempts < maxAttempts) {
              attempts++;
              console.log(`\n⏳ ATTEMPT ${attempts}/${maxAttempts}:`);
              console.log(`   Still waiting for: ${result.currently_running?.join(', ') || 'unknown'}`);
              
              await new Promise(resolve => setTimeout(resolve, 60000));  // Wait 1 minute
              result = await checkAllWorkflowsAndChecks();
            }

            // ✅ CRITICAL: Only proceed if ALL completed OR timeout
            if (!result.all_complete) {
              console.log(`\n⏰ TIMEOUT after ${attempts} attempts (${maxAttempts} minutes).`);
              console.log(`⚠️  Still running: ${result.currently_running?.join(', ') || 'none'}`);
              console.log(`❌ STOPPING - Will not send notification until all workflows and checks complete!`);
              
              // ❌ EXIT WITHOUT SENDING NOTIFICATION
              process.exit(1);
            } else {
              console.log(`\n🎉 SUCCESS: All workflows and checks completed after ${attempts} attempts! (${attempts} minutes)`);
              console.log(`✅ READY TO SEND NOTIFICATION`);
            }

            return result;

      # ✅ CRITICAL: This step only runs if ALL workflows and checks completed
      - name: Send Complete Status Notification
        env:
          TEAMS_WEBHOOK_URL: ${{ secrets.TEAMS_WEBHOOK_URL }}
        run: |
          pr_json='${{ steps.pr_info.outputs.result }}'
          completion_json='${{ steps.wait_for_completion.outputs.result }}'

          PR_NUMBER=$(echo "$pr_json" | jq -r '.number // "N/A"')
          PR_TITLE=$(echo "$pr_json" | jq -r '.title // "N/A"' | sed 's/"/\\"/g' | sed "s/'/\\'/g")
          PR_URL=$(echo "$pr_json" | jq -r '.url // "N/A"')
          PR_AUTHOR=$(echo "$pr_json" | jq -r '.author // "N/A"')
          HEAD_SHA=$(echo "$pr_json" | jq -r '.head_sha // "N/A"')
          SHORT_SHA=$(echo "$HEAD_SHA" | cut -c1-7)

          # Safe parsing with defaults
          TOTAL_COUNT=$(echo "$completion_json" | jq -r '.total_count // 0')
          EXTERNAL_COUNT=$(echo "$completion_json" | jq -r '.external_count // 0')
          WORKFLOW_COUNT=$(echo "$completion_json" | jq -r '.workflow_count // 0')
          CHECK_COUNT=$(echo "$completion_json" | jq -r '.check_count // 0')
          STATUS_COUNT=$(echo "$completion_json" | jq -r '.status_count // 0')

          WORKFLOWS=$(echo "$completion_json" | jq -c '.workflows // []')
          
          SUCCESS_COUNT=$(echo "$WORKFLOWS" | jq '[.[] | select(.status == "success")] | length')
          FAILED_COUNT=$(echo "$WORKFLOWS" | jq '[.[] | select(.status == "failure")] | length')
          CANCELLED_COUNT=$(echo "$WORKFLOWS" | jq '[.[] | select(.status == "cancelled")] | length')
          SKIPPED_COUNT=$(echo "$WORKFLOWS" | jq '[.[] | select(.status == "skipped")] | length')
          NEUTRAL_COUNT=$(echo "$WORKFLOWS" | jq '[.[] | select(.status == "neutral")] | length')

          echo "✅ All workflows and checks completed! Building notification..."
          echo "Total: $TOTAL_COUNT (Workflows: $WORKFLOW_COUNT, Checks: $CHECK_COUNT, Status: $STATUS_COUNT, External: $EXTERNAL_COUNT)"
          echo "Results: Success: $SUCCESS_COUNT, Failed: $FAILED_COUNT, Cancelled: $CANCELLED_COUNT, Skipped: $SKIPPED_COUNT"

          WORKFLOW_TEXTBLOCKS=""
          if [ "$TOTAL_COUNT" -gt 0 ]; then
             echo "Building all workflows and checks with colored status text..."
             WORKFLOW_COUNT_TOTAL=$(echo "$WORKFLOWS" | jq 'length')
             for i in $(seq 0 $((WORKFLOW_COUNT_TOTAL - 1))); do
               NAME=$(echo "$WORKFLOWS" | jq -r ".[$i].name")
               STATUS=$(echo "$WORKFLOWS" | jq -r ".[$i].status | ascii_upcase")
               URL=$(echo "$WORKFLOWS" | jq -r ".[$i].url")
               REPO=$(echo "$WORKFLOWS" | jq -r ".[$i].repository // \"\"")
               TYPE=$(echo "$WORKFLOWS" | jq -r ".[$i].type // \"\"")
               
               # Escape quotes for JSON
               NAME_SAFE=$(echo "$NAME" | sed 's/"/\\"/g')
               
               # ✅ ENHANCED Color coding for status with specific colors
               case "$STATUS" in
                 "SUCCESS")
                   STATUS_COLOR="Good"
                   STATUS_WEIGHT="Bolder"
                   ;;
                 "FAILURE")
                   STATUS_COLOR="Attention"
                   STATUS_WEIGHT="Bolder"
                   ;;
                 "CANCELLED")
                   STATUS_COLOR="Warning"
                   STATUS_WEIGHT="Bolder"
                   ;;
                 "SKIPPED")
                   STATUS_COLOR="Accent"
                   STATUS_WEIGHT="Bolder"
                   ;;
                 "NEUTRAL")
                   STATUS_COLOR="Default"
                   STATUS_WEIGHT="Bolder"
                   ;;
                 "ERROR")
                   STATUS_COLOR="Attention"
                   STATUS_WEIGHT="Bolder"
                   ;;
                 *)
                   STATUS_COLOR="Default"
                   STATUS_WEIGHT="Bolder"
                   ;;
               esac
               
               # Show type and repository information
               if [[ "$TYPE" == *"external"* ]] && [ "$REPO" != "$GITHUB_REPOSITORY" ] && [ -n "$REPO" ] && [ "$REPO" != "null" ]; then
                 REPO_TEXT=" [$REPO]"
               elif [[ "$TYPE" == *"status"* ]]; then
                 REPO_TEXT=" [status]"
               elif [[ "$TYPE" == *"check"* ]]; then
                 REPO_TEXT=" [check]"
               else
                 REPO_TEXT=""
               fi
               
               # ✅ Enhanced RichTextBlock with better color formatting
               COMBINED_BLOCK='{
                 "type": "RichTextBlock",
                 "inlines": [
                   {
                     "type": "TextRun",
                     "text": "• "
                   },
                   {
                     "type": "TextRun",
                     "text": "'$NAME_SAFE'",
                     "selectAction": {
                       "type": "Action.OpenUrl",
                       "url": "'$URL'"
                     }
                   },
                   {
                     "type": "TextRun",
                     "text": " - "
                   },
                   {
                     "type": "TextRun",
                     "text": "'$STATUS'",
                     "color": "'$STATUS_COLOR'",
                     "weight": "'$STATUS_WEIGHT'",
                     "size": "Medium"
                   },
                   {
                     "type": "TextRun",
                     "text": "'$REPO_TEXT'",
                     "color": "Dark",
                     "size": "Small"
                   }
                 ],
                 "spacing": "None"
               }'
               
               if [ -z "$WORKFLOW_TEXTBLOCKS" ]; then
                 WORKFLOW_TEXTBLOCKS="$COMBINED_BLOCK"
               else
                 WORKFLOW_TEXTBLOCKS="$WORKFLOW_TEXTBLOCKS,$COMBINED_BLOCK"
               fi
             done
           else
            WORKFLOW_TEXTBLOCKS='{
              "type": "TextBlock",
              "text": "• No workflows or checks found",
              "wrap": true,
              "size": "Small",
              "color": "Default"
              }'
            fi

          # ✅ Determine overall status (ALL completed at this point)
          if [ "$FAILED_COUNT" -gt 0 ]; then
            ICON="🔴"
            STATUS_TEXT="❌ SOME CHECKS FAILED"
            COLOR="Attention"
          elif [ "$CANCELLED_COUNT" -gt 0 ]; then
            ICON="🟠"
            STATUS_TEXT="🟠 SOME CHECKS CANCELLED"
            COLOR="Warning"
          elif [ "$SUCCESS_COUNT" -gt 0 ] && [ "$FAILED_COUNT" -eq 0 ]; then
            ICON="🟢"
            STATUS_TEXT="✅ ALL CHECKS PASSED"
            COLOR="Good"
          elif [ "$TOTAL_COUNT" -eq 0 ]; then
            ICON="⚪"
            STATUS_TEXT="⚪ NO WORKFLOWS OR CHECKS FOUND"
            COLOR="Default"
          else
            ICON="🟡"
            STATUS_TEXT="⚠️ MIXED RESULTS"
            COLOR="Warning"
          fi

          # Completion status (all completed)
          LOCAL_COUNT=$((TOTAL_COUNT - EXTERNAL_COUNT))
          COMPLETION_TEXT="✅ All $TOTAL_COUNT workflows and checks completed ($LOCAL_COUNT local + $EXTERNAL_COUNT external)"

          # Escape for JSON
          TITLE_SAFE="$ICON PR #$PR_NUMBER (Main) — $PR_TITLE"

          echo "📤 Sending complete Teams notification..."

          # Create final status notification
          cat > card.json <<EOF
          {
            "type": "message",
            "attachments": [
              {
                 "contentType": "application/vnd.microsoft.card.adaptive",
                 "content": {
                    "\$schema": "http://adaptivecards.io/schemas/adaptive-card.json",
                    "type": "AdaptiveCard",
                    "version": "1.4",
                    "body": [
                      {
                         "type": "Container",
                         "items": [
                           {
                              "type": "TextBlock",
                              "text": "$TITLE_SAFE",
                              "wrap": true,
                              "weight": "Bolder",
                              "size": "Medium"
                           },
                           {
                              "type": "TextBlock",
                              "text": "$STATUS_TEXT",
                              "wrap": true,
                              "weight": "Bolder",
                              "color": "$COLOR",
                              "size": "Large"
                           },
                           {
                              "type": "FactSet",
                              "facts": [
                                    { "title": "Author:", "value": "$PR_AUTHOR" },
                                    { "title": "Branch:", "value": "main" },
                                    { "title": "Commit:", "value": "$SHORT_SHA" },
                                    { "title": "Status:", "value": "$COMPLETION_TEXT" },
                                    { "title": "Total:", "value": "$TOTAL_COUNT" },
                                    { "title": "🏗️ Workflows:", "value": "$WORKFLOW_COUNT" },
                                    { "title": "✔️ Checks:", "value": "$CHECK_COUNT" },
                                    { "title": "📊 Status:", "value": "$STATUS_COUNT" },
                                    { "title": "🌐 External:", "value": "$EXTERNAL_COUNT" },
                                    { "title": "✅ Passed:", "value": "$SUCCESS_COUNT" },
                                    { "title": "❌ Failed:", "value": "$FAILED_COUNT" },
                                    { "title": "🟠 Cancelled:", "value": "$CANCELLED_COUNT" },
                                    { "title": "⏭️ Skipped:", "value": "$SKIPPED_COUNT" }
                               ]
                              },
                              {
                                 "type": "TextBlock",
                                 "text": "**Complete Results (Workflows + Checks + External):**",
                                 "wrap": true,
                                 "weight": "Bolder",
                                 "size": "Small",
                                 "spacing": "Medium"
                              },
                              $WORKFLOW_TEXTBLOCKS
                            ]
                          }
                        ],
                        "actions": [
                          {
                             "type": "Action.OpenUrl",
                             "title": "View PR",
                             "url": "$PR_URL"
                          },
                          {
                             "type": "Action.OpenUrl",
                             "title": "View All Checks",
                             "url": "https://github.com/${{ github.repository }}/pull/$PR_NUMBER/checks"
                          },
                          {
                              "type": "Action.OpenUrl",
                              "title": "View Commit",
                              "url": "https://github.com/${{ github.repository }}/commit/$HEAD_SHA"
                          }
                       ]
                   }
              }
             ]
          }
          EOF
           
           curl -s -H "Content-Type: application/json" \
            -d @card.json \
            "$TEAMS_WEBHOOK_URL" || (echo "❌ Failed to send Teams message" && exit 1)
           
           echo "✅ Complete Teams notification sent successfully!"
